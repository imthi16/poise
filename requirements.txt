# POISE runtime dependencies.
# The repo is designed to run OFF-DEVICE (laptop/CI/Kaggle) in mock mode and
# ON-DEVICE (Jetson) in real mode. Heavy/optional deps are grouped so the core
# (config, telemetry-mock, control, simulator, env, serving, storage) installs
# and tests green without the model or the board.

# --- Core (always required) ---
numpy>=1.26
scipy>=1.11
PyYAML>=6.0
python-dotenv>=1.0
pydantic>=2.5

# --- Serving / metrics ---
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
prometheus-client>=0.20

# --- RL (off-device training + env) ---
gymnasium>=0.29
# Pick ONE maintained PPO implementation for training (Kaggle):
stable-baselines3>=2.3        # or: cleanrl

# --- Testing ---
pytest>=8.0

# --- Adaptive engine (ON-DEVICE / GPU box) — heavy, install separately ---
# torch>=2.2                  # install the CUDA build matching the Jetson/JetPack
# transformers>=4.44
# accelerate>=0.30
# bitsandbytes>=0.43          # only for POISE_DTYPE=bnb-4bit

# --- LayerSkip adaptation (LoRA early-exit fine-tune; on GPU) ---
# peft>=0.11                  # only needed for poise/adaptation (CLAUDE.md §9 a)

# --- Baselines (optional) ---
# llama-cpp-python>=0.2.79    # GGUF Q4_K_M static baseline
# tensorrt-llm                # optional fixed-depth baseline

# --- Telemetry (on Jetson only) ---
# jetson-stats>=4.2           # provides `jtop`; tegrastats is a system binary

# --- RAG demo (public/synthetic data only) ---
# faiss-cpu>=1.8
# langgraph>=0.1
